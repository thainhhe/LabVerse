<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="12dp"
    android:layout_marginTop="12dp"
    app:cardElevation="2dp"
    app:cardCornerRadius="8dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <ImageView
                android:id="@+id/iv_priority"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:layout_marginStart="8dp"
                android:visibility="gone"
                app:srcCompat="@drawable/bg_priority_tag"
                app:tint="?attr/colorError"
                tools:visibility="visible" />
            <TextView
                android:id="@+id/tv_paper_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_toStartOf="@id/iv_priority"
                android:textAppearance="@style/TextAppearance.MaterialComponents.Subtitle1"
                android:textStyle="bold"
                android:maxLines="2"
                android:ellipsize="end"
                tools:text="Generative Adversarial Networks (GANs)" />
        </RelativeLayout>

        <TextView
            android:id="@+id/tv_paper_authors"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textAppearance="@style/TextAppearance.MaterialComponents.Body2"
            android:maxLines="1"
            android:ellipsize="end"
            tools:text="Ian Goodfellow, Jean Pouget-Abadie, Mehdi Mirza..." />

        <com.google.android.material.chip.Chip
            android:id="@+id/chip_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="To Read"
            app:chipBackgroundColor="@color/status_unread"
            android:textColor="@android:color/white"
            style="@style/Widget.MaterialComponents.Chip.Action" />
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>